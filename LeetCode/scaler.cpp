#include<iostream>
#include<cmath>
using namespace std;
int main(){
    int inptno,remainder;
    cout<<"Enter the number";
    cin>>inptno;
    int size= sizeof(inptno);
    while (size)
    {
        remainder=inptno%10;
        inptno=floor(inptno.10);
        
    }
    
}